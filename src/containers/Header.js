import React from "react"; //I could ignore this, no longer a requirement

const Header = () => {
    return (
        <div className="ui fixed menu">
            <div className="ui container center">
                <h2>FakeShop</h2>
            </div>
        </div>
    );
};

export default Header;